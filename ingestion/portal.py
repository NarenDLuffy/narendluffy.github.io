    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
L    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
v    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
m    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
g    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
d    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
c    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
v    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
y    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
m    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
h    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
c    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
3    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
G    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
P    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
P    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
m    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
g    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
v    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
c    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
.    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
T    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
h    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
g    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
h    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
:    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
/    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
/    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
.    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
3    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
g    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
.    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
g    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
/    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
?    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
b    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
d    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
=    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
3    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
7    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
3    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
&    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
S    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
u    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
b    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
T    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
B    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
=    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
3    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
7    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
9    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
b    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
c    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
k    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
d    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
b    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
y    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
R    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
E    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
S    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
T    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
v    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
c    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
h    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
u    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
v    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
y    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
m    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
g    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
c    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
h    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
c    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
b    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
d    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
y    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
,    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
w    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
h    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
u    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
h    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
v    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
d    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
,    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
c    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
,    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
c    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
u    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
y    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
d    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
m    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
z    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
.    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
R    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
A    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
N    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
1    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
c    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
h    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
c    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
b    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
d    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
y    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
3    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
7    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
9    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
.    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
N    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
h    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
g    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
h    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
c    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
c    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
g    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
m    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
g    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
:    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
w    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
h    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
v    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
h    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
v    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
c    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
u    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
u    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
b    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
h    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
d    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
,    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
u    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
u    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
m    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
g    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
u    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
m    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
c    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
y    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
.    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
m    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
_    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
_    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
u    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
u    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
_    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
_    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
m    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
m    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
c    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
v    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
m    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
m    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
m    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
d    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
c    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
m    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
d    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
c    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
m    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
d    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
m    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
m    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
d    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
m    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
,    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
m    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
z    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
m    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
y    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
g    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
m    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
A    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
y    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
m    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
z    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
m    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
Z    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
I    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
,    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
v    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
b    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
_    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
m    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
z    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
m    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
q    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
u    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
M    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
E    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
E    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
T    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
I    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
N    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
G    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
S    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
_    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
E    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
N    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
D    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
P    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
O    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
I    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
N    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
T    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
=    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
h    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
:    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
/    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
/    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
.    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
3    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
g    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
.    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
g    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
/    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
w    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
b    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
v    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
c    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
/    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
R    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
/    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
M    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
g    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
.    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
v    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
c    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
/    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
G    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
M    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
g    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
R    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
A    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
N    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
1    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
_    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
T    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
B    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
_    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
I    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
D    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
=    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
3    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
7    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
9    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
F    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
T    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
P    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
_    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
R    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
O    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
O    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
T    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
=    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
h    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
:    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
/    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
/    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
w    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
w    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
w    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
.    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
3    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
g    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
.    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
g    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
/    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
/    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
g    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
_    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
/    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
W    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
G    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
1    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
_    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
R    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
L    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
1    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
U    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
S    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
E    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
R    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
_    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
A    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
G    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
E    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
N    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
T    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
=    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
(    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
M    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
z    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
/    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
5    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
.    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
0    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
(    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
X    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
1    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
1    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
;    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
L    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
u    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
x    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
x    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
8    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
6    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
_    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
6    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
4    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
)    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
A    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
W    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
b    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
K    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
/    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
5    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
3    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
7    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
.    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
3    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
6    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
(    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
K    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
H    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
T    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
M    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
L    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
,    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
k    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
G    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
c    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
k    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
)    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
C    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
h    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
m    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
/    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
1    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
2    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
4    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
.    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
0    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
S    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
/    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
5    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
3    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
7    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
.    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
3    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
6    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
)    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
T    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
I    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
T    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
L    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
E    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
_    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
R    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
E    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
=    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
.    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
c    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
m    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
(    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
R    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
A    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
N    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
1    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
#    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
(    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
?    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
P    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
<    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
u    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
m    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
b    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
>    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
\    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
d    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
+    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
)    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
(    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
?    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
P    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
<    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
u    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
x    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
>    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
[    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
\    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
]    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
?    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
b    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
)    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
?    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
,    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
.    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
I    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
)    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
_    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
=    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
q    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
u    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
.    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
S    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
(    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
)    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
_    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
.    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
h    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
d    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
.    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
u    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
d    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
(    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
{    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
U    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
A    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
g    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
:    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
U    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
S    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
E    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
R    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
_    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
A    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
G    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
E    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
N    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
T    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
,    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
R    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
:    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
h    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
:    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
/    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
/    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
.    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
3    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
g    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
.    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
g    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
/    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
}    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
)    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
@    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
d    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
c    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
c    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
P    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
M    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
g    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
:    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
O    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
m    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
g    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
x    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
c    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
y    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
h    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
d    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
c    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
b    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
.    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
_    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
d    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
:    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
m    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
:    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
u    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
m    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
b    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
:    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
y    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
:    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
_    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
d    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
:    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
d    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
_    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
d    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
:    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
m    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
z    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
:    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
c    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
y    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
:    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
|    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
N    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
c    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
u    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
y    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
:    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
|    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
N    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
d    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
_    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
u    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
:    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
|    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
N    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
w    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
:    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
d    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
c    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
[    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
,    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
A    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
y    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
]    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
@    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
y    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
d    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
u    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
g    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
(    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
)    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
>    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
:    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
u    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
1    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
{    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
.    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
u    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
m    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
b    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
}    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
b    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
.    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
y    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
=    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
=    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
b    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
1    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
{    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
.    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
u    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
m    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
b    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
}    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
d    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
c    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
h    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
_    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
m    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
g    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
(    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
:    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
,    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
d    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
:    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
)    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
>    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
[    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
P    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
M    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
g    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
]    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
:    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
E    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
v    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
y    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
R    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
A    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
N    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
1    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
m    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
g    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
h    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
k    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
w    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
b    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
u    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
h    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
g    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
v    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
w    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
d    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
w    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
.    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
y    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
d    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
=    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
{    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
g    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
M    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
g    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
I    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
u    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
:    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
{    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
S    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
R    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
w    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
:    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
0    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
,    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
R    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
u    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
P    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
P    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
g    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
:    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
2    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
0    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
0    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
,    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
S    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
B    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
y    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
:    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
D    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
,    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
S    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
A    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
c    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
d    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
g    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
:    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
T    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
u    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
,    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
S    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
D    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
:    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
{    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
}    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
0    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
0    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
:    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
0    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
0    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
:    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
0    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
0    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
,    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
E    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
d    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
D    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
:    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
{    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
d    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
}    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
0    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
0    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
:    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
0    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
0    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
:    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
0    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
0    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
,    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
T    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
b    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
:    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
[    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
R    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
A    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
N    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
1    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
_    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
T    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
B    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
_    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
I    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
D    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
]    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
,    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
I    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
c    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
u    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
d    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
C    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
h    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
d    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
T    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
b    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
:    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
F    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
,    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
I    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
c    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
u    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
d    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
N    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
T    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
B    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
M    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
g    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
:    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
F    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
,    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
R    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
c    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
:    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
,    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
R    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
g    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
d    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
:    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
F    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
,    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
}    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
}    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
=    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
_    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
.    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
(    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
M    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
E    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
E    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
T    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
I    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
N    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
G    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
S    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
_    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
E    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
N    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
D    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
P    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
O    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
I    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
N    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
T    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
,    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
j    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
=    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
y    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
d    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
,    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
m    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
u    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
=    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
4    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
5    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
)    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
.    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
_    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
_    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
u    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
(    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
)    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
u    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
[    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
m    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
m    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
(    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
_    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
_    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
m    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
g    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
(    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
w    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
)    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
w    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
.    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
j    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
(    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
)    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
)    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
m    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
]    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
d    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
_    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
_    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
m    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
g    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
(    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
w    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
:    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
d    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
c    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
[    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
,    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
A    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
y    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
]    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
)    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
>    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
P    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
M    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
g    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
|    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
N    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
:    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
=    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
(    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
w    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
.    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
g    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
(    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
T    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
)    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
w    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
.    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
g    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
(    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
S    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
h    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
T    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
)    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
)    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
.    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
(    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
)    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
m    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
c    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
h    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
=    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
T    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
I    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
T    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
L    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
E    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
_    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
R    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
E    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
.    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
c    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
h    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
(    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
.    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
c    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
(    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
3    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
G    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
P    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
P    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
,    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
3    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
G    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
P    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
P    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
)    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
)    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
m    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
c    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
h    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
:    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
u    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
N    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
#    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
g    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
,    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
c    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
v    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
,    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
h    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
c    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
h    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
c    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
b    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
d    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
u    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
m    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
b    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
=    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
(    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
m    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
c    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
h    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
.    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
g    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
u    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
(    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
u    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
m    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
b    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
)    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
)    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
k    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
d    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
=    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
b    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
m    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
c    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
h    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
.    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
g    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
u    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
(    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
u    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
x    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
)    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
g    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
u    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
u    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
P    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
M    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
g    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
(    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
_    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
d    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
=    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
(    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
w    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
[    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
I    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
d    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
]    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
)    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
,    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
m    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
=    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
R    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
A    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
N    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
1    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
#    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
{    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
u    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
m    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
b    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
}    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
b    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
k    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
d    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
=    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
=    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
b    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
R    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
A    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
N    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
1    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
#    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
{    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
u    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
m    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
b    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
}    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
,    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
u    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
m    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
b    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
=    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
u    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
m    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
b    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
,    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
y    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
=    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
k    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
d    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
,    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
_    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
d    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
=    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
(    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
w    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
[    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
S    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
D    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
]    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
)    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
[    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
:    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
1    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
0    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
]    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
,    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
d    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
_    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
d    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
=    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
(    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
w    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
[    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
E    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
d    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
D    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
]    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
)    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
[    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
:    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
1    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
0    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
]    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
,    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
m    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
z    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
=    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
v    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
_    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
m    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
z    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
(    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
w    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
.    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
g    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
(    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
S    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
T    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
m    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
Z    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
)    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
)    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
,    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
c    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
y    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
=    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
_    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
c    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
_    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
c    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
(    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
w    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
.    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
g    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
(    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
L    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
c    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
)    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
)    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
,    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
c    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
u    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
y    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
=    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
w    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
.    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
g    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
(    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
C    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
u    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
y    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
)    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
N    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
,    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
d    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
_    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
u    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
=    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
_    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
h    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
_    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
d    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
(    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
w    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
.    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
g    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
(    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
M    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
g    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
D    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
c    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
U    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
R    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
L    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
)    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
)    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
,    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
w    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
=    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
w    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
,    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
)    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
d    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
_    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
c    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
_    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
c    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
(    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
c    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
:    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
|    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
N    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
)    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
>    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
|    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
N    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
:    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
c    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
c    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
.    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
w    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
(    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
)    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
{    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
,    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
,    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
b    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
d    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
}    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
:    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
u    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
N    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
u    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
.    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
u    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
b    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
(    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
\    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
+    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
M    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
A    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
$    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
,    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
,    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
c    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
)    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
.    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
(    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
)    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
N    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
d    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
_    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
h    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
_    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
d    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
(    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
u    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
:    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
|    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
N    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
)    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
>    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
|    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
N    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
:    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
u    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
:    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
u    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
N    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
d    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
=    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
u    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
.    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
c    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
(    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
h    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
:    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
/    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
/    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
.    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
3    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
g    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
.    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
g    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
/    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
,    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
h    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
:    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
/    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
/    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
w    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
w    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
w    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
.    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
3    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
g    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
.    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
g    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
/    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
/    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
)    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
d    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
=    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
d    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
.    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
c    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
(    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
h    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
:    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
/    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
/    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
.    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
3    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
g    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
.    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
g    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
/    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
,    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
h    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
:    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
/    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
/    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
w    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
w    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
w    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
.    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
3    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
g    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
.    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
g    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
/    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
/    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
)    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
u    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
d    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
d    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
.    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
d    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
w    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
h    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
(    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
/    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
)    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
d    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
+    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
/    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
#    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
m    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
z    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
u    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
#    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
T    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
h    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
W    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
d    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
w    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
y    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
b    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
u    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
c    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
h    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
(    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
G    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
M    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
T    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
+    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
0    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
9    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
:    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
0    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
0    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
)    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
S    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
u    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
.    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
T    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
h    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
c    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
y    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
#    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
m    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
d    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
m    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
c    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
h    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
d    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
g    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
h    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
I    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
A    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
N    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
A    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
d    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
b    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
,    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
w    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
h    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
c    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
v    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
#    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
w    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
h    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
u    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
k    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
u    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
b    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
.    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
_    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
Z    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
O    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
N    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
E    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
S    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
_    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
B    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
Y    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
_    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
C    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
I    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
T    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
Y    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
:    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
d    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
c    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
[    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
,    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
]    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
=    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
{    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
}    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
_    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
z    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
v    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
b    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
_    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
m    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
z    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
(    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
)    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
:    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
_    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
Z    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
O    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
N    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
E    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
S    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
_    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
B    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
Y    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
_    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
C    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
I    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
T    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
Y    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
.    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
d    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
u    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
(    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
_    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
z    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
.    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
(    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
/    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
,    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
1    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
)    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
[    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
1    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
]    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
.    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
c    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
(    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
_    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
,    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
)    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
.    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
w    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
(    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
)    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
,    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
_    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
z    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
)    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
O    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
F    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
F    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
S    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
E    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
T    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
_    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
R    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
E    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
=    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
.    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
c    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
m    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
(    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
G    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
M    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
T    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
(    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
[    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
+    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
]    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
)    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
(    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
\    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
d    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
{    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
1    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
,    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
2    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
}    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
)    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
[    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
:    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
.    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
]    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
(    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
\    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
d    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
{    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
2    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
}    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
)    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
,    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
.    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
I    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
)    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
d    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
v    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
_    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
m    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
z    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
(    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
b    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
:    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
|    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
N    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
,    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
b    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
c    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
k    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
:    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
=    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
U    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
T    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
C    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
)    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
>    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
:    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
b    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
:    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
u    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
b    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
c    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
k    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
c    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
=    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
b    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
.    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
(    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
)    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
,    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
1    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
)    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
[    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
1    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
]    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
c    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
y    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
[    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
c    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
.    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
(    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
)    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
.    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
w    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
(    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
)    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
c    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
c    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
.    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
(    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
,    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
)    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
c    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
.    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
(    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
)    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
]    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
:    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
z    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
=    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
_    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
Z    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
O    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
N    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
E    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
S    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
_    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
B    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
Y    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
_    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
C    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
I    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
T    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
Y    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
.    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
g    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
(    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
c    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
y    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
)    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
z    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
:    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
u    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
z    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
#    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
N    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
k    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
w    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
c    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
y    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
:    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
b    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
c    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
k    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
y    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
z    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
c    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
u    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
y    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
h    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
d    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
.    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
m    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
=    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
O    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
F    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
F    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
S    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
E    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
T    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
_    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
R    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
E    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
.    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
c    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
h    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
(    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
b    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
)    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
m    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
:    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
g    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
=    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
1    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
m    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
.    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
g    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
u    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
(    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
1    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
)    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
=    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
=    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
+    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
1    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
w    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
d    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
=    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
g    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
*    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
(    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
(    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
m    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
.    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
g    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
u    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
(    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
2    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
)    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
)    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
*    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
3    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
6    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
0    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
0    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
+    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
(    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
m    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
.    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
g    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
u    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
(    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
3    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
)    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
)    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
*    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
6    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
0    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
)    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
w    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
=    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
d    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
m    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
.    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
w    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
(    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
m    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
z    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
.    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
u    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
c    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
)    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
z    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
d    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
(    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
v    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
b    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
_    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
m    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
z    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
(    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
)    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
)    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
:    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
y    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
:    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
=    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
w    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
.    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
m    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
z    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
(    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
Z    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
I    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
(    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
z    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
)    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
)    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
.    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
u    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
c    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
(    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
)    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
x    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
c    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
E    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
x    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
c    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
:    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
#    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
g    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
m    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
:    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
c    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
v    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
d    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
v    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
c    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
u    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
N    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
d    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
(    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
.    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
_    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
c    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
d    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
(    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
)    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
)    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
=    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
=    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
w    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
d    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
:    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
u    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
z    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
u    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
b    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
c    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
k    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
#    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
m    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
g    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
d    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
c    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
H    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
R    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
E    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
F    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
_    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
R    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
E    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
=    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
.    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
c    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
m    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
(    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
'    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
h    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
=    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
(    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
?    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
P    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
<    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
h    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
>    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
[    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
^    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
]    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
+    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
)    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
'    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
,    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
.    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
I    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
)    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
d    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
_    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
d    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
(    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
u    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
:    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
)    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
>    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
[    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
]    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
:    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
F    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
d    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
d    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
U    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
R    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
L    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
d    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
3    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
G    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
P    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
P    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
d    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
c    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
y    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
g    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
.    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
y    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
:    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
=    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
_    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
.    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
g    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
(    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
u    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
,    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
m    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
u    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
=    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
4    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
5    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
)    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
.    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
_    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
_    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
u    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
(    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
)    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
x    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
c    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
q    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
u    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
.    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
R    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
q    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
u    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
E    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
x    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
c    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
:    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
u    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
[    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
]    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
:    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
[    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
]    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
=    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
[    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
]    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
m    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
H    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
R    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
E    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
F    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
_    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
R    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
E    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
.    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
d    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
(    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
.    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
x    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
)    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
:    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
h    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
=    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
m    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
.    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
g    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
u    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
(    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
h    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
)    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
h    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
.    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
w    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
h    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
(    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
?    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
)    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
/    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
/    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
h    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
:    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
c    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
u    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
b    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
u    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
=    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
h    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
h    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
.    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
w    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
h    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
(    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
h    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
)    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
h    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
:    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
/    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
/    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
w    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
w    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
w    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
.    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
3    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
g    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
.    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
g    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
+    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
h    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
b    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
u    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
.    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
(    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
/    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
)    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
!    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
=    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
u    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
.    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
(    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
/    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
)    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
d    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
b    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
u    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
.    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
w    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
h    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
(    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
u    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
)    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
:    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
.    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
d    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
(    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
b    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
u    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
)    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
u    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
d    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
(    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
(    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
)    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
)    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
d    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
c    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
h    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
_    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
g    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
d    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
_    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
c    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
v    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
(    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
d    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
_    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
u    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
:    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
)    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
-    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
>    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
[    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
u    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
[    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
,    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
]    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
]    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
:    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
(    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
c    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
d    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
,    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
)    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
w    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
m    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
h    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
m    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
g    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
'    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
u    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
b    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
h    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
d    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
g    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
d    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
.    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
c    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
v    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
,    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
y    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
.    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
y    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
:    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
=    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
_    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
.    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
g    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
(    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
{    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
d    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
_    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
u    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
}    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
A    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
g    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
d    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
/    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
g    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
d    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
.    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
c    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
v    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
"    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
,    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
m    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
u    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
=    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
4    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
5    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
)    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
.    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
u    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
_    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
c    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
d    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
!    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
=    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
2    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
0    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
0    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
.    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
x    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
.    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
(    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
)    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
:    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
u    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
[    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
]    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
x    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
c    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
q    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
u    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
.    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
R    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
q    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
u    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
E    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
x    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
c    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
:    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
u    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
[    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
]    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
w    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
:    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
[    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
u    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
[    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
,    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
]    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
]    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
=    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
[    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
]    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
w    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
c    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
v    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
.    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
d    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
(    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
.    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
S    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
g    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
I    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
O    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
(    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
.    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
x    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
)    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
)    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
:    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
f    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
l    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
(    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
w    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
)    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
>    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
=    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
2    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
d    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
w    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
[    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
0    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
]    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
.    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
(    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
)    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
:    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
w    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
.    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
a    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
d    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
(    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
(    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
w    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
[    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
0    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
]    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
.    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
(    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
)    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
,    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
w    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
[    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
1    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
]    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
.    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
i    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
p    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
(    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
)    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
)    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
)    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
e    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
t    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
u    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
n    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
     # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
r    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
o    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
w    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
s    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"

    # No known city name: use the stated fixed UTC offset instead of guessing
    # a region (Etc/GMT signs are inverted by POSIX convention).
    m = OFFSET_RE.search(label)
    if m and int(m.group(3)) == 0:
        hours = int(m.group(2))
        if hours == 0:
            return "UTC"
        return f"Etc/GMT{'-' if m.group(1) == '+' else '+'}{hours}"
